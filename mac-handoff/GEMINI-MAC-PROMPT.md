# Gemini Mac prompt

Continue my twelve-app portfolio from the GitHub branches `codex/mac-handoff-20260923`. These branches contain the Android source/artwork/walkthrough checkpoint from 23 September 2026. The default branches of existing repos do not necessarily contain this work. I have authorized local development, simulator/device testing and preparation of reviewable iOS archives, not public deployment or store submission.

First read `mac-handoff/PORTFOLIO-MAC.md` and each app's AGENTS.md and current roadmap. Clone the repositories into a fresh Mac folder using `bash mac-handoff/clone-on-mac.sh` (GitHub authentication may be needed for private repositories). Preserve my existing checkouts. Confirm all twelve handoff branches are present and report missing toolchains or access. Use the actual app paths in the table. Work in separate per-app branches after checkout; do not overwrite the Android checkpoint or change its app identities.

Prioritize Being Tea, Literally Even, Wonder Notes, Brick by Brick, Life Giving Communication and Terraseeds for iOS. Then inspect Cooked, Smile, Aware, Noo, Personas and AliaSpaces. Keep Website/Social/Local modes for AliaSpaces and the actual owner command-center scope for Personas. Some iOS projects are stubs or absent: implement those hosts where feasible and report the distinction. For Cooked the mobile app is `apps/cooked-mobile`, not the website or cookbook root. It includes the nutrition tracker and persistent pantry; its fixture meal planner is still explicitly separate. Never invent personal nutrient absorption or storage-decay percentages.

Port the four-page purpose/features/example/setup walkthrough, add an accessible in-app replay entry, and preserve completion/checklist state. Use the editable SVGs in mac-handoff/brand-assets for iOS icon catalogs and coordinated launch art. Keep approved content and game canon boundaries. Run meaningful tests, simulator builds and device QA where available: large text, keyboard/safe areas, offline launch, process restart, share/import and error handling. Inspect existing scripts and locked dependencies before selecting build commands. Do not overwrite branded Android resources with a broad platform regeneration.

Keep passwords, signing keys and health/journal records out of chat and Git. Ask me to use native account/signing interfaces when necessary. The original Android test signing keys remain on Windows; generating a new key on the Mac cannot update those installed copies. Do not uninstall my phone apps to bypass a key mismatch. Use Apple signing only through the authorized account UI. No paid services, account permission changes, automatic private-data uploads, public publishing or store submission.

Return per-app source branches/commits, exact commands, test results, screenshots, iOS artifact paths and remaining blockers. Distinguish build success from production/store readiness. Bring the resulting commits back to Codex for reconciliation.
