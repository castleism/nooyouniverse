{
  "name": "noo-private-observation-log",
  "private": true,
  "version": "0.1.0",
  "description": "Mission-linked private observation tracker (debug milestone, not a store release)",
  "scripts": {
    "extract-missions": "node scripts/extract-missions.mjs",
    "sync-assets": "node scripts/sync-web-assets.mjs",
    "bundle-site": "node scripts/bundle-site-copy.mjs",
    "prepare-web": "npm run extract-missions && npm run sync-assets && npm run bundle-site",
    "test": "npm run prepare-web && node --test tests/catalog.test.mjs tests/store.test.mjs tests/hub-and-site.test.mjs",
    "smoke": "npm run prepare-web && node tests/ui-smoke.mjs",
    "serve": "npm run prepare-web && python3 -m http.server 4173 --directory web"
  },
  "devDependencies": {
    "puppeteer-core": "^23.11.1"
  }
}
