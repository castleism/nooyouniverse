# Test fixtures only

These JSON files are **synthetic test payloads**. They are not user data, not sensor logs, and not clinical records.

The shipped app starts empty. Do not copy these files into the Android assets as sample user history.
